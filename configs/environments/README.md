# Environment Configs

Environment configs define non-secret runtime defaults.

Secrets do not belong here. Use environment variables or a secret manager for:

- database passwords
- API keys
- cloud credentials
- model provider credentials

Current environments:

- `local.yaml`
- `dev.yaml`

