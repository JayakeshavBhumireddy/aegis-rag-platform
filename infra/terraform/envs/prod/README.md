# Production Terraform Environment

Production will use remote state, stricter IAM boundaries, encrypted storage, and protected apply workflows.

We will not add production resources until dev and staging patterns are stable.

